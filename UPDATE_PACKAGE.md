# Update package.json

Add these to devDependencies:

```json
"devDependencies": {
  "tailwindcss": "^3.4.0",
  "postcss": "^8.4.32",
  "autoprefixer": "^10.4.16",
  ...existing deps
}
```

Then run: `npm install`
